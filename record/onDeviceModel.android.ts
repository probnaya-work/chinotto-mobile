/**
 * How a transcript read by this phone's own recogniser is labelled on Android: only ever
 * one that `modules/chinotto-voice` accepted as unable to reach the network.
 */
export const LOCAL_READING_MODEL = 'android-on-device';
